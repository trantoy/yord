# Agent Nodes

First-class agent entities in the tree. Agent component + ContextOf component. Agents can observe sibling terminal output, spawn sub-agent children, communicate via Anthropic API (or other providers). Conversation history persisted in SQLite.

## Priority

M4.

## Referenced from

- `docs/design.md` — section 10, milestones M4
