# Remote Projects (SSH)

SSH into a server, run terminal/editor nodes there. Node config would have a remote: SSHConfig field. Architecture supports it but not in scope yet. Would need PTY spawning on remote host.

## Priority

Future.

## Referenced from

- `docs/design.md` — open question #3
- `docs/refs/projects/waveterm.md` — ConnInterface abstraction over local/SSH/WSL
