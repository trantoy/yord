# TUI File Manager IPC (yazi)

yazi has a plugin API. Should Yord talk to yazi over its IPC to know which file is focused (to open it in the editor node)? Or is that over-engineering?

## Priority

Future. Design question, not decided.

## Referenced from

- `docs/design.md` — open question #1
